/// @description Insert description here
// You can write your code in this editor

if (buttonFrame != undefined)
	instance_destroy(buttonFrame);

buttonFrame = instance_create_depth(x, y, 4, ButtonFrame);
